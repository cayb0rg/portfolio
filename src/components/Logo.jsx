export default function Logo(props) {
    return (
        <img
            alt={props.alt}
            src={props.src}
            className="logo w-10 h-10 mt-auto mb-auto ml-0 mr-0"
        />
    )
}
